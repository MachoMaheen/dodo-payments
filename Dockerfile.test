@echo off
:: Simple test Dockerfile
FROM rust:1.75 as builder
WORKDIR /app
RUN echo 'fn main() { println!("Hello from Rust!"); }' > hello.rs
RUN rustc hello.rs
FROM debian:bullseye-slim
WORKDIR /app
COPY --from=builder /app/hello /app/hello
CMD ["/app/hello"]
